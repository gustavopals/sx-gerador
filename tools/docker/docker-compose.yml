name: sxgerador

services:
  postgres:
    image: postgres:16-alpine
    container_name: sxgerador-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: sxgerador
      POSTGRES_USER: sxgerador
      POSTGRES_PASSWORD: sxgerador
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - '5432:5432'
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U sxgerador -d sxgerador']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  pgadmin:
    image: dpage/pgadmin4:8
    container_name: sxgerador-pgadmin
    restart: unless-stopped
    profiles:
      - admin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@sxgerador.local
      PGADMIN_DEFAULT_PASSWORD: sxgerador
      PGADMIN_CONFIG_SERVER_MODE: 'False'
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: 'False'
    ports:
      - '5050:80'
    depends_on:
      postgres:
        condition: service_healthy
